{
  "name": "claude-session-merge",
  "version": "1.0.0",
  "description": "Merge & sync Claude desktop-app sessions across accounts (macOS, zero-dependency Bun CLI, dry-run by default).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sebryu/claude-session-merge.git"
  },
  "bin": {
    "claude-session-merge": "./claude-session-merge.ts"
  },
  "scripts": {
    "start": "bun claude-session-merge.ts"
  }
}
